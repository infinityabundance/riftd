[package]
name = "rift-nat"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
thiserror = "1"
tokio = { version = "1", features = ["net", "sync", "time", "macros"] }
rift-core = { path = "../rift-core" }
rift-metrics = { path = "../rift-metrics" }
tracing = "0.1"
