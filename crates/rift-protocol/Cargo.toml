[package]
name = "rift-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = "1"
blake3 = "1"
hex = "0.4"
rand = "0.7"
serde = { version = "1", features = ["derive"] }
thiserror = "1"
rift-core = { path = "../rift-core" }
